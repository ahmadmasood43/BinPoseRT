# @package _global_
# A5 — CNOS masks · MegaPose · single view: estimator dependency. Comparator row (slips first).
defaults:
  - override /segmenter: cnos
  - override /estimator: megapose

experiment: A5
